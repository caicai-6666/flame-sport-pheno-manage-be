# AGENTS.md

## 1. 文件目的与适用范围

本文件是本仓库内所有智能体参与开发、审查、排障和文档维护时必须遵守的项目级规范，适用于仓库根目录及其全部子目录。

进入项目后，不得直接依据通用经验开始编码。必须先了解项目事实、确认任务边界，并遵循本文件规定的阅读、开发、验证和文档流程。若子目录后续存在更具体的 `AGENTS.md`，则子目录规范可补充或收紧本文件；发生冲突时，以距离目标文件最近的规范为准，但不得降低本文件中的安全与质量要求。

规范用语：

- “必须”“禁止”表示不可省略的硬性要求。
- “应”表示默认遵守；如需偏离，必须说明理由及影响。
- “可以”表示可根据实际任务选择。

---

## 2. 开始工作前的必读内容

每个智能体开始辅助项目开发前，必须按以下顺序完成阅读：

1. 完整阅读本文件，不能只依据任务描述或历史上下文行动。
2. 完整阅读 `description/document-style.md`，所有新增或修改的项目文档都必须符合其中的撰写规范。
3. 阅读 `description/project.md`，了解项目目标、业务范围、术语、总体架构以及其中规定的文档阅读顺序。
4. 查阅 `description/README.md` 文档地图，并按 `description/project.md` 的指引阅读与本次任务直接相关的业务、接口、模块和数据表文档。
5. 检查目标代码、相邻模块、测试、依赖配置和已有实现，确认项目实际采用的技术栈与编码习惯。
6. 检查当前工作区状态，识别并保留用户或其他智能体尚未提交的改动。

必须明确区分 `description/project.md` 与 `description/db/project.md`：前者是项目总览和文档阅读入口，后者只是 `project` 数据表说明，不能互相替代。不得自行臆测缺失的产品规则、接口契约或技术选型；影响实现方向且无法从仓库确认时，应先向用户说明并请求确认。

---

## 3. 项目事实与信息优先级

项目目前处于初始化阶段。现有内容只能证明这是一个企业运动平台后端项目，并包含 MySQL 业务表设计文档；示例 `main.py` 不能被视为已经确定的应用架构或最终编码风格。

判断项目事实时，按以下优先级处理：

1. 用户在当前任务中的明确要求。
2. 目标目录适用的 `AGENTS.md`。
3. `description/project.md` 及其指定的业务文档。
4. `description/` 下与功能直接相关的其他文档。
5. 已运行代码、测试和配置所体现的实际行为。
6. 注释、示例和个人经验。

若文档、代码和任务要求互相矛盾，不得静默选择其中一种。应先核实是否属于历史实现、未完成迁移或文档过期；无法安全判断时，向用户报告冲突、影响和建议方案。

---

## 4. 通用开发原则

### 4.1 任务范围

- 只修改完成当前任务所必需的文件，禁止顺手重构无关模块或批量格式化整个仓库。
- 保留工作区中不属于当前任务的已有改动；不得覆盖、回滚或删除来源不明的内容。
- 优先复用项目已有抽象、公共能力和约定，不重复实现等价逻辑。
- 不得为了让测试通过而删除断言、绕过校验、吞掉异常或硬编码测试结果。
- 不得引入没有实际价值的新依赖。新增依赖前必须确认现有依赖无法合理完成任务，并说明用途。

### 4.2 代码设计

- 设计必须遵循高内聚、低耦合和单一职责原则。
- 按项目已经采用的分层组织代码；在架构尚未确定时，不得擅自创建庞大的自定义框架。
- 业务规则应集中在职责明确的领域或服务组件内，不得散落在控制器、路由、数据库访问和序列化代码中。
- 上层逻辑应依赖稳定抽象，基础设施细节不得反向侵入业务核心。
- 根据真实变化点选择设计模式，例如策略、工厂、适配器、仓储或依赖注入；禁止为了“使用设计模式”而增加无意义的接口、继承层级和转发代码。
- 函数和类应短小且职责明确。出现多重业务目的、复杂条件分支或难以命名的代码块时，应拆分为可测试的单元。
- 命名必须表达业务含义，避免含糊缩写、无意义编号以及 `data`、`info`、`temp` 等缺少上下文的名称。

### 4.3 函数中文注释（硬性要求）

开发或修改每个函数、方法、构造函数、异步任务、事件处理器和生命周期钩子时，必须在其声明之前写关键中文注释。新增函数未带关键中文注释，视为功能未完成。

注释要求：

- 注释应说明函数的核心职责和关键业务规则，不能只是把函数名翻译成中文。
- 涉及边界条件、状态变化、事务、幂等、权限、异常或非直观算法时，应说明原因和约束。
- 参数、返回值和异常仅在不能从类型与命名清楚理解时补充，避免堆砌无信息量模板。
- Python 装饰器存在时，关键中文注释应位于装饰器组之前；其他语言应放在函数声明紧邻位置。
- 修改已有无中文注释的函数时，只要本次改动触及其逻辑，就必须同时补充符合要求的中文注释。
- `lambda` 或匿名回调只应用于简单表达式；承载业务逻辑时必须改为具名函数并添加中文注释。

Python 示例：

```python
# 校验用户是否达到赛季报名条件，并保证重复提交不会创建多条参与记录。
def enroll_user(user_id: str, season_id: int) -> Enrollment:
    ...
```

注释应保持与实现同步。过期、误导或仅描述“做了什么”而不解释关键意图的注释必须更新。

### 4.4 接口与兼容性

- 修改公开 API、配置项、消息格式、公共类型或持久化行为前，必须检查现有调用方和兼容性。
- 除非任务明确要求破坏性变更，否则应保持已有字段语义、错误协议和默认行为兼容。
- 输入必须在系统边界完成校验；错误信息应可定位但不得泄露敏感信息或内部实现细节。
- 时间、时区、金额、积分、状态枚举和小数精度必须采用明确的数据类型与统一口径，禁止依赖模糊的隐式转换。

### 4.5 数据一致性与并发

- 涉及多表写入、余额/积分变化、状态流转、进度累计和结算时，必须明确事务边界。
- 对重复请求、任务重试、并发更新和部分失败进行分析；需要时实现幂等约束、唯一键检查、锁或乐观并发控制。
- 不得以静默覆盖、先删后建等方式掩盖一致性问题。
- 查询应避免明显的 N+1、无界全表读取和无必要的重复聚合；优化必须基于实际调用路径，不能擅自改变业务口径。

---

## 5. `description/db` 数据库文档保护规则（硬性要求）

`description/db/` 是整个业务的数据库表描述文档，是理解表结构、字段语义、约束和业务口径的只读事实来源。

- 没有用户明确指令时，只允许读取 `description/db/`，禁止新增、修改、移动、重命名或删除其中任何文件。
- “顺便完善文档”“保持文档同步”或智能体自行判断需要优化，都不构成修改授权。
- 功能实现与数据库文档不一致时，不得擅自修改数据库文档来适配代码；应报告差异并确认正确口径。
- 未经明确要求，不得执行 DDL、迁移、数据修复、数据删除或其他可能改变数据库结构与业务数据的操作。
- 如任务明确要求修改数据库设计，应先读取所有关联表文档，分析外键、索引、历史数据、回滚方案和兼容性，再进行最小范围变更。
- 一般功能文档可以引用或链接 `description/db/` 中的表说明，但禁止复制后形成容易失效的第二份表结构定义。

---

## 6. 功能文档规范（硬性要求）

相应功能开发完成后，必须根据功能所处层级，在 `description/` 的对应位置创建或更新功能文档。代码与功能文档共同构成功能交付，缺少应有文档时不得宣称任务完成。

所有新增或修改的功能文档必须遵循 `description/document-style.md`，充分利用 Markdown 的层级标题、列表、强调、引用、表格、代码块和图表等合适的结构化语法，保证结构清晰、留白适度且易于阅读。

功能文档按以下层级存放；已有更具体的目录约定时优先沿用已有约定：

| 功能层级 | 默认文档位置 | 主要内容 |
| --- | --- | --- |
| 对外 HTTP/RPC 接口 | `description/api/` | 路径、请求、响应、错误码、权限、示例 |
| 应用用例/服务编排 | `description/application/` | 用例目标、流程、事务、依赖、失败处理 |
| 核心业务规则/领域能力 | `description/domain/` | 术语、实体、状态流转、不变量、计算规则 |
| 数据访问/外部系统集成 | `description/infrastructure/` | 依赖边界、适配方式、超时重试、降级策略 |
| 定时任务/异步任务 | `description/job/` | 触发条件、处理范围、幂等、重试、监控 |
| 跨层完整功能 | `description/features/` | 用户目标、端到端流程及各层文档链接 |

文档组织要求：

- 目录尚不存在时，应在真正产生相应功能后按需创建，禁止只建立无内容的空目录。
- 一个功能横跨多个层级时，应分别记录各层职责；可增加一份 `description/features/` 总览串联相关文档，避免把所有细节堆进单个文件。
- 文件名使用稳定、清晰的小写英文 `kebab-case` 业务名称，以连字符 `-` 分隔单词，禁止使用下划线分隔；标题和正文以中文为主，代码标识、协议字段和专有名词保留其真实名称。
- 更新已有功能时，优先更新原文档，不得为同一功能创建 `new`、`final`、`v2` 等重复文档；确有版本并存需求时需说明版本边界。
- 文档必须描述当前最终行为，不写成开发过程流水账，也不能只写“新增了某接口”之类无法辅助后续开发的结论。

一份功能文档至少应包含与该功能相关的以下内容：

1. 功能目标与适用范围。
2. 核心业务规则、前置条件和不变量。
3. 主要流程以及关键状态变化。
4. 输入、输出、异常和边界条件。
5. 所依赖的模块、接口及 `description/db/` 表文档链接。
6. 事务、幂等、并发、权限或安全要求（如适用）。
7. 验证方式、关键测试场景和已知限制。

纯拼写修复、格式调整或不改变行为的内部重构通常无需新建功能文档，但若已有文档中的路径、名称、职责或行为受到影响，必须同步更新对应的非数据库文档。

---

## 7. 测试与验证

- 新功能必须包含覆盖核心成功路径的测试，并覆盖与风险相称的失败、边界、权限、事务和并发场景。
- 修复缺陷时，应优先添加能够复现问题并防止回归的测试。
- 测试应验证可观察行为，不应过度绑定私有实现细节。
- 不得依赖执行顺序、真实生产服务、不可控时间或随机结果；需要时使用可注入时钟、固定种子、桩或测试替身。
- 完成修改后，应先运行最贴近改动范围的测试、类型检查和静态检查，再视项目规模扩大验证范围。
- 只能运行仓库中能够确认存在的命令，不得虚构构建、测试或格式化命令。项目尚未提供相关工具时，应进行可行的最小验证，并在交付说明中明确未执行项及原因。
- 测试失败时必须判断是本次改动、既有问题还是环境限制，禁止隐瞒失败或将未运行描述为通过。

---

## 8. 安全、日志与配置

- 禁止在代码、测试、文档、日志或提交内容中写入真实密钥、令牌、密码、Cookie、个人敏感信息和生产数据。
- 配置应通过项目既定方式注入，并提供安全的非生产默认值；不得把生产地址或危险开关设为默认值。
- 日志应包含定位问题所需的业务上下文，但不得记录认证凭证、完整敏感请求体或无脱敏的个人信息。
- 涉及认证、授权、文件上传、外部 URL、SQL、命令执行和反序列化时，必须考虑越权、注入、路径穿越、SSRF 及资源滥用风险。
- 外部依赖调用必须考虑超时、失败语义和必要的重试；重试必须有上限，并确保不会导致重复扣减、重复发奖等副作用。

---

## 9. 标准工作流程

每项开发任务应遵循以下流程：

1. **理解**：完成必读内容，复述任务边界，识别不明确的业务规则和高风险操作。
2. **调查**：定位相关代码、测试、文档、调用方和数据表，只基于仓库事实形成方案。
3. **设计**：明确模块职责、数据流、接口、异常、事务和兼容性；选择满足需求的最简单可靠设计。
4. **实现**：进行最小范围修改，为每个新增或触及的函数补充关键中文注释，保持风格一致。
5. **测试**：补充或更新测试并执行相关验证，记录无法验证的部分。
6. **文档**：在 `description/` 对应层级创建或更新功能文档；无明确授权时不得改动 `description/db/`。
7. **自检**：检查差异，确认无调试代码、无秘密信息、无无关改动、无遗漏文档，并核对兼容性。
8. **交付**：简洁说明完成内容、关键设计、文档位置、验证结果和仍需用户决策的事项。

---

## 10. 完成标准

只有同时满足以下条件，才能认为开发任务完成：

- 实现符合用户要求和已确认的业务规则。
- 所有新增或修改的函数均有准确的关键中文注释。
- 代码职责清晰，符合高内聚、低耦合原则，没有无必要的设计复杂度。
- 相关测试已补充并通过；未能执行的验证已如实说明。
- 对应层级的功能文档已创建或更新，且能够帮助后续开发者理解功能。
- `description/db/` 未在缺少明确指令的情况下被改动。
- 未引入无关变更、敏感信息、明显兼容性问题或未说明的风险。
